# ShramikSaathi — Dependencies
# Install: pip install -r requirements.txt
# Note: Install torch separately with your CUDA version first:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128  (RTX 5070 Ti / Blackwell)
#   pip install torch --index-url https://download.pytorch.org/whl/cu126  (RTX 4090 / Ada)
#   pip install torch --index-url https://download.pytorch.org/whl/cu124  (RTX 3090 / Ampere)

# Core inference
transformers==4.46.3
peft==0.13.2
bitsandbytes>=0.48.0
accelerate>=1.1.0
safetensors>=0.7.0

# Retrieval
sentence-transformers>=5.3.0
faiss-cpu>=1.13.0
rank-bm25>=0.2.2

# Demo UI
gradio>=4.0.0

# Training (only needed on training machine)
trl==0.12.2
datasets>=4.0.0

# Evaluation (only needed for running evals)
groq>=1.0.0
ragas>=0.4.0

# Utilities
python-dotenv>=1.0.0
numpy>=1.26.0
regex>=2024.0.0
